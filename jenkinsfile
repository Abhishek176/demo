pipeline {
    agent any
    tools {
        terraform 'terraform'
    }
    stages {
        stage ("Terraform init") {
            steps {
                sh "terraform init"
                sh "terraform apply -auto-approve"
            }
        }
    }
}
